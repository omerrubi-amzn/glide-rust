// Copyright Valkey GLIDE Project Contributors - SPDX Identifier: Apache-2.0
//
// The method table (names, signatures, and doc comments) is derived from the
// vendored redis-rs fork's `implement_commands!` invocation
// (`redis` crate v0.25.2, BSD-3-Clause license, valkey-io/valkey-glide rev
// 052ae4e; see licenses/LICENSE.redis-rs and NOTICE).
// Each method delegates to the fork's generated `Cmd::<name>()` constructor,
// so the wire encoding is byte-identical to redis-rs.
//
// GENERATED by tools/gen_compat_commands.py — do not edit by hand.
//! GLIDE-owned drop-in replacements for the redis-rs `AsyncCommands` /
//! `Commands` traits, with **native copy behavior**.
//!
//! Signature-identical to the fork's traits (same names, same generic
//! parameter order — existing call sites and turbofish annotations compile
//! unchanged), but each method builds the command via the fork's own
//! `Cmd::<name>()` constructor and hands it **by value** to glide-core,
//! skipping the `&Cmd` -> clone tax of the `ConnectionLike` dispatch path.
//! A 10MB `SET` through these traits copies the payload exactly as many
//! times as the native GLIDE API.
//!
//! The fork's original traits remain implemented (via
//! [`redis::aio::ConnectionLike`]) and reachable as
//! [`crate::redis::AsyncCommands`] / [`crate::redis::Commands`] for code that
//! needs the literal redis-rs traits (e.g. generic functions bounded on
//! them). Do not import both flavors in one scope — method calls would be
//! ambiguous.

use redis::{
    Cmd, Direction, Expiry, FromRedisValue, LposOptions, RedisFuture, RedisResult, SetOptions,
    ToRedisArgs, Value, cmd, from_owned_redis_value,
};

/// The **async** redis-rs command surface with native copy behavior.
///
/// Drop-in for the fork's `redis::AsyncCommands`: same method names,
/// signatures, and semantics; commands are sent by value (no per-call `Cmd`
/// clone). Implemented by [`crate::GlideClient`] and
/// [`crate::GlideClusterClient`].
pub trait AsyncCommands: redis::aio::ConnectionLike + Send + Sized {
    /// Send an already-built command **by value** (no clone). This is the
    /// single required method; every typed command delegates to it. Also
    /// useful directly as a zero-extra-copy escape hatch for custom commands
    /// with large payloads.
    fn glide_send_owned<'a>(&'a mut self, cmd: Cmd) -> RedisFuture<'a, Value>;

    /// Get the value of a key.  If key is a vec this becomes an `MGET`.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::get(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get values of keys
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mget<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::mget(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets all keys matching pattern
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn keys<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::keys(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the string value of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::set(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the string value of a key with options.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_options<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
        options: SetOptions,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::set_options(key, value, options);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets multiple keys to their values.
    #[allow(deprecated)]
    #[deprecated(since = "0.22.4", note = "Renamed to mset() to reflect Redis name")]
    #[inline]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_multiple<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        items: &'a [(K, V)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::set_multiple(items);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets multiple keys to their values.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mset<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        items: &'a [(K, V)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::mset(items);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the value and expiration of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_ex<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
        seconds: u64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::set_ex(key, value, seconds);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the value and expiration in milliseconds of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pset_ex<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
        milliseconds: u64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pset_ex(key, value, milliseconds);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the value of a key, only if the key does not exist
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_nx<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::set_nx(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets multiple keys to their values failing if at least one already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mset_nx<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        items: &'a [(K, V)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::mset_nx(items);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the string value of a key and return its old value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getset<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::getset(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get a range of bytes/substring from the value of a key. Negative values provide an offset from the end of the value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getrange<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        from: isize,
        to: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::getrange(key, from, to);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Overwrite the part of the value stored in key at the specified offset.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn setrange<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        offset: isize,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::setrange(key, offset, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Delete one or more keys.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn del<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::del(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine if a key exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn exists<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::exists(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine the type of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn key_type<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::key_type(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set a key's time to live in seconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn expire<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        seconds: i64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::expire(key, seconds);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the expiration for a key as a UNIX timestamp.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn expire_at<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        ts: i64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::expire_at(key, ts);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set a key's time to live in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pexpire<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        ms: i64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pexpire(key, ms);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Set the expiration for a key as a UNIX timestamp in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pexpire_at<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        ts: i64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pexpire_at(key, ts);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove the expiration from a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn persist<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::persist(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the expiration time of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn ttl<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::ttl(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the expiration time of a key in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pttl<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pttl(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the value of a key and set expiration
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get_ex<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        expire_at: Expiry,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::get_ex(key, expire_at);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the value of a key and delete it
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get_del<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::get_del(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Rename a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rename<'a, K: ToRedisArgs + Send + Sync + 'a, N: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        new_key: N,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rename(key, new_key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Rename a key, only if the new key does not exist.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rename_nx<'a, K: ToRedisArgs + Send + Sync + 'a, N: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        new_key: N,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rename_nx(key, new_key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Unlink one or more keys.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn unlink<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::unlink(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Append a value to a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn append<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::append(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Increment the numeric value of a key by the given amount.  This
    /// issues a `INCRBY` or `INCRBYFLOAT` depending on the type.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn incr<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        delta: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::incr(key, delta);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Decrement the numeric value of a key by the given amount.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn decr<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        delta: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::decr(key, delta);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets or clears the bit at offset in the string value stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn setbit<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        offset: usize,
        value: bool,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::setbit(key, offset, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the bit value at offset in the string value stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getbit<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        offset: usize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::getbit(key, offset);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Count set bits in a string.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bitcount<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bitcount(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Count set bits in a string in a range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bitcount_range<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: usize,
        end: usize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bitcount_range(key, start, end);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Perform a bitwise AND between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_and<'a, D: ToRedisArgs + Send + Sync + 'a, S: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bit_and(dstkey, srckeys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Perform a bitwise OR between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_or<'a, D: ToRedisArgs + Send + Sync + 'a, S: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bit_or(dstkey, srckeys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Perform a bitwise XOR between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_xor<'a, D: ToRedisArgs + Send + Sync + 'a, S: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bit_xor(dstkey, srckeys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Perform a bitwise NOT of the key (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_not<'a, D: ToRedisArgs + Send + Sync + 'a, S: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        srckey: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bit_not(dstkey, srckey);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the length of the value stored in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn strlen<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::strlen(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets a single (or multiple) fields from a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hget<'a, K: ToRedisArgs + Send + Sync + 'a, F: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        field: F,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hget(key, field);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Deletes a single (or multiple) fields from a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hdel<'a, K: ToRedisArgs + Send + Sync + 'a, F: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        field: F,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hdel(key, field);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets a single field in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        F: ToRedisArgs + Send + Sync + 'a,
        V: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        field: F,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hset(key, field, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets a single field in a hash if it does not exist.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset_nx<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        F: ToRedisArgs + Send + Sync + 'a,
        V: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        field: F,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hset_nx(key, field, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets a multiple fields in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset_multiple<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        F: ToRedisArgs + Send + Sync + 'a,
        V: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        items: &'a [(F, V)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hset_multiple(key, items);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Increments a value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hincr<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        F: ToRedisArgs + Send + Sync + 'a,
        D: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        field: F,
        delta: D,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hincr(key, field, delta);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Checks if a field in a hash exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hexists<'a, K: ToRedisArgs + Send + Sync + 'a, F: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        field: F,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hexists(key, field);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets all the keys in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hkeys<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hkeys(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets all the values in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hvals<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hvals(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets all the fields and values in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hgetall<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hgetall(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Gets the length of a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hlen<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::hlen(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pop an element from a list, push it to another list
    /// and return it; or block until one is available
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blmove<'a, S: ToRedisArgs + Send + Sync + 'a, D: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        srckey: S,
        dstkey: D,
        src_dir: Direction,
        dst_dir: Direction,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::blmove(srckey, dstkey, src_dir, dst_dir, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pops `count` elements from the first non-empty list key from the list of
    /// provided key names; or blocks until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blmpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        timeout: f64,
        numkeys: usize,
        key: K,
        dir: Direction,
        count: usize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::blmpop(timeout, numkeys, key, dir, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove and get the first element in a list, or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::blpop(key, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove and get the last element in a list, or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn brpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::brpop(key, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pop a value from a list, push it to another list and return it;
    /// or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn brpoplpush<'a, S: ToRedisArgs + Send + Sync + 'a, D: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        srckey: S,
        dstkey: D,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::brpoplpush(srckey, dstkey, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get an element from a list by its index.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lindex<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        index: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lindex(key, index);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Insert an element before another element in a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn linsert_before<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        P: ToRedisArgs + Send + Sync + 'a,
        V: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        pivot: P,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::linsert_before(key, pivot, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Insert an element after another element in a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn linsert_after<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        P: ToRedisArgs + Send + Sync + 'a,
        V: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        pivot: P,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::linsert_after(key, pivot, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the length of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn llen<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::llen(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pop an element a list, push it to another list and return it
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lmove<'a, S: ToRedisArgs + Send + Sync + 'a, D: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        srckey: S,
        dstkey: D,
        src_dir: Direction,
        dst_dir: Direction,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lmove(srckey, dstkey, src_dir, dst_dir);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pops `count` elements from the first non-empty list key from the list of
    /// provided key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lmpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        numkeys: usize,
        key: K,
        dir: Direction,
        count: usize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lmpop(numkeys, key, dir, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns the up to `count` first elements of the list stored at key.
    ///
    /// If `count` is not specified, then defaults to first element.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: Option<core::num::NonZeroUsize>,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lpop(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the index of the first matching value of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpos<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
        options: LposOptions,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lpos(key, value, options);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Insert all the specified values at the head of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpush<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lpush(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Inserts a value at the head of the list stored at key, only if key
    /// already exists and holds a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpush_exists<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lpush_exists(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the specified elements of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lrange<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lrange(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes the first count occurrences of elements equal to value
    /// from the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lrem<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: isize,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lrem(key, count, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Trim an existing list so that it will contain only the specified
    /// range of elements specified.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn ltrim<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::ltrim(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Sets the list element at index to value
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lset<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        index: isize,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::lset(key, index, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns the up to `count` last elements of the list stored at key
    ///
    /// If `count` is not specified, then defaults to last element.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: Option<core::num::NonZeroUsize>,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rpop(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Pop a value from a list, push it to another list and return it.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpoplpush<'a, K: ToRedisArgs + Send + Sync + 'a, D: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        dstkey: D,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rpoplpush(key, dstkey);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Insert all the specified values at the tail of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpush<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rpush(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Inserts value at the tail of the list stored at key, only if key
    /// already exists and holds a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpush_exists<'a, K: ToRedisArgs + Send + Sync + 'a, V: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        value: V,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::rpush_exists(key, value);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Add one or more members to a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sadd<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sadd(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the number of members in a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn scard<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::scard(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Subtract multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sdiff<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sdiff(keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Subtract multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sdiffstore<'a, D: ToRedisArgs + Send + Sync + 'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sdiffstore(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Intersect multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sinter<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sinter(keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Intersect multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sinterstore<'a, D: ToRedisArgs + Send + Sync + 'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sinterstore(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine if a given value is a member of a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sismember<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sismember(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine if given values are members of a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smismember<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        members: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::smismember(key, members);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get all the members in a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smembers<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::smembers(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Move a member from one set to another.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smove<
        'a,
        S: ToRedisArgs + Send + Sync + 'a,
        D: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        srckey: S,
        dstkey: D,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::smove(srckey, dstkey, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove and return a random member from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn spop<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::spop(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get one random member from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srandmember<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::srandmember(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get multiple random members from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srandmember_multiple<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: usize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::srandmember_multiple(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove one or more members from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srem<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::srem(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Add multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sunion<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sunion(keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Add multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sunionstore<'a, D: ToRedisArgs + Send + Sync + 'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        keys: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::sunionstore(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Add one member to a sorted set, or update its score if it already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zadd<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        S: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        member: M,
        score: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zadd(key, member, score);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Add multiple members to a sorted set, or update its score if it already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zadd_multiple<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        S: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        items: &'a [(S, M)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zadd_multiple(key, items);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the number of members in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zcard<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(&'a mut self, key: K) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zcard(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Count the members in a sorted set with scores within the given values.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zcount<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zcount(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Increments the member in a sorted set at key by delta.
    /// If the member does not exist, it is added with delta as its score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zincr<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        D: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        member: M,
        delta: D,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zincr(key, member, delta);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using SUM as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore<'a, D: ToRedisArgs + Send + Sync + 'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using MIN as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_min<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore_min(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using MAX as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_max<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore_max(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zinterstore`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zinterstore_min`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_min_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore_min_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zinterstore_max`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_max_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zinterstore_max_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Count the number of members in a sorted set between a given lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zlexcount<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zlexcount(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns the member with the highest score in a sorted set.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzpopmax<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bzpopmax(key, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the highest scores in a sorted set
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zpopmax<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zpopmax(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns the member with the lowest score in a sorted set.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzpopmin<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        timeout: f64,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bzpopmin(key, timeout);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the lowest scores in a sorted set
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zpopmin<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zpopmin(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the highest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzmpop_max<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        timeout: f64,
        keys: &'a [K],
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bzmpop_max(timeout, keys, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the highest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zmpop_max<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        keys: &'a [K],
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zmpop_max(keys, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the lowest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzmpop_min<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        timeout: f64,
        keys: &'a [K],
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::bzmpop_min(timeout, keys, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Removes and returns up to count members with the lowest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zmpop_min<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        keys: &'a [K],
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zmpop_min(keys, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return up to count random members in a sorted set (or 1 if `count == None`)
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrandmember<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: Option<isize>,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrandmember(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return up to count random members in a sorted set with scores
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrandmember_withscores<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrandmember_withscores(key, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by index
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrange<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrange(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by index with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrange_withscores<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrange_withscores(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebylex<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebylex(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by lexicographical
    /// range with offset and limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebylex_limit<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebylex_limit(key, min, max, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebylex<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebylex(key, max, min);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by lexicographical
    /// range with offset and limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebylex_limit<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebylex_limit(key, max, min, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebyscore(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_withscores<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebyscore_withscores(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_limit<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebyscore_limit(key, min, max, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with limit with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_limit_withscores<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrangebyscore_limit_withscores(key, min, max, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine the index of a member in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrank<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrank(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove one or more members from a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrem<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        members: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrem(key, members);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove all members in a sorted set between the given lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrembylex<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrembylex(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove all members in a sorted set within the given indexes.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zremrangebyrank<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zremrangebyrank(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Remove all members in a sorted set within the given scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrembyscore<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrembyscore(key, min, max);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by index, with scores
    /// ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrange<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrange(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by index, with scores
    /// ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrange_withscores<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrange_withscores(key, start, stop);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebyscore(key, max, min);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_withscores<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebyscore_withscores(key, max, min);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_limit<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebyscore_limit(key, max, min, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return a range of members in a sorted set, by score with limit with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_limit_withscores<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        MM: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrangebyscore_limit_withscores(key, max, min, offset, count);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Determine the index of a member in a sorted set, with scores ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrank<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zrevrank(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the score associated with the given member in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zscore<'a, K: ToRedisArgs + Send + Sync + 'a, M: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        member: M,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zscore(key, member);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Get the scores associated with multiple members in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zscore_multiple<
        'a,
        K: ToRedisArgs + Send + Sync + 'a,
        M: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        key: K,
        members: &'a [M],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zscore_multiple(key, members);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using SUM as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore<'a, D: ToRedisArgs + Send + Sync + 'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using MIN as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_min<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore_min(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using MAX as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_max<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore_max(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zunionstore`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zunionstore_min`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_min_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore_min_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// [`Commands::zunionstore_max`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_max_weights<
        'a,
        D: ToRedisArgs + Send + Sync + 'a,
        K: ToRedisArgs + Send + Sync + 'a,
        W: ToRedisArgs + Send + Sync + 'a,
        RV,
    >(
        &'a mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::zunionstore_max_weights(dstkey, keys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Adds the specified elements to the specified HyperLogLog.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfadd<'a, K: ToRedisArgs + Send + Sync + 'a, E: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
        element: E,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pfadd(key, element);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Return the approximated cardinality of the set(s) observed by the
    /// HyperLogLog at key(s).
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfcount<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pfcount(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Merge N different HyperLogLogs into a single one.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfmerge<'a, D: ToRedisArgs + Send + Sync + 'a, S: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::pfmerge(dstkey, srckeys);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Posts a message to the given channel.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn publish<'a, K: ToRedisArgs + Send + Sync + 'a, E: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        channel: K,
        message: E,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::publish(channel, message);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the encoding of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_encoding<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::object_encoding(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the time in seconds since the last access of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_idletime<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::object_idletime(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the logarithmic access frequency counter of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_freq<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::object_freq(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Returns the reference count of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_refcount<'a, K: ToRedisArgs + Send + Sync + 'a, RV>(
        &'a mut self,
        key: K,
    ) -> RedisFuture<'a, RV>
    where
        RV: FromRedisValue,
    {
        let cmd = Cmd::object_refcount(key);
        Box::pin(async move { from_owned_redis_value(self.glide_send_owned(cmd).await?) })
    }

    /// Incrementally iterate the keys space.
    #[inline]
    fn scan<RV: FromRedisValue>(&mut self) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("SCAN");
        c.cursor_arg(0);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate the keys space for keys matching a pattern.
    #[inline]
    fn scan_match<P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        pattern: P,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("SCAN");
        c.cursor_arg(0).arg("MATCH").arg(pattern);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate hash fields and associated values.
    #[inline]
    fn hscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("HSCAN");
        c.arg(key).cursor_arg(0);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate hash fields and associated values for field
    /// names matching a pattern.
    #[inline]
    fn hscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("HSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate set elements.
    #[inline]
    fn sscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("SSCAN");
        c.arg(key).cursor_arg(0);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate set elements for elements matching a pattern.
    #[inline]
    fn sscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("SSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate sorted set elements.
    #[inline]
    fn zscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("ZSCAN");
        c.arg(key).cursor_arg(0);
        Box::pin(async move { c.iter_async(self).await })
    }

    /// Incrementally iterate sorted set elements for elements matching a
    /// pattern.
    #[inline]
    fn zscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisFuture<'_, redis::AsyncIter<'_, RV>> {
        let mut c = cmd("ZSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        Box::pin(async move { c.iter_async(self).await })
    }
}

/// The **blocking** redis-rs command surface with native copy behavior.
///
/// Drop-in for the fork's `redis::Commands` — see [`AsyncCommands`].
/// Implemented by [`crate::sync::SyncGlideClient`] and
/// [`crate::sync::SyncGlideClusterClient`].
///
/// These methods block on the internal runtime and **must not be called from
/// within an async context** (doing so panics with tokio's "cannot block the
/// current thread from within a runtime"); use [`AsyncCommands`] on the async
/// clients there instead.
#[cfg(feature = "sync")]
pub trait Commands: redis::ConnectionLike + Sized {
    /// Send an already-built command **by value** (no clone). This is the
    /// single required method; every typed command delegates to it.
    fn glide_send_owned_sync(&mut self, cmd: Cmd) -> RedisResult<Value>;

    /// Get the value of a key.  If key is a vec this becomes an `MGET`.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::get(key))?)
    }

    /// Get values of keys
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mget<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::mget(key))?)
    }

    /// Gets all keys matching pattern
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn keys<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::keys(key))?)
    }

    /// Set the string value of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::set(key, value))?)
    }

    /// Set the string value of a key with options.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_options<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
        options: SetOptions,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::set_options(key, value, options))?)
    }

    /// Sets multiple keys to their values.
    #[allow(deprecated)]
    #[deprecated(since = "0.22.4", note = "Renamed to mset() to reflect Redis name")]
    #[inline]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_multiple<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        items: &'a [(K, V)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::set_multiple(items))?)
    }

    /// Sets multiple keys to their values.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mset<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        items: &'a [(K, V)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::mset(items))?)
    }

    /// Set the value and expiration of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_ex<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
        seconds: u64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::set_ex(key, value, seconds))?)
    }

    /// Set the value and expiration in milliseconds of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pset_ex<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
        milliseconds: u64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pset_ex(
            key,
            value,
            milliseconds,
        ))?)
    }

    /// Set the value of a key, only if the key does not exist
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn set_nx<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::set_nx(key, value))?)
    }

    /// Sets multiple keys to their values failing if at least one already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn mset_nx<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        items: &'a [(K, V)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::mset_nx(items))?)
    }

    /// Set the string value of a key and return its old value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getset<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::getset(key, value))?)
    }

    /// Get a range of bytes/substring from the value of a key. Negative values provide an offset from the end of the value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getrange<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        from: isize,
        to: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::getrange(key, from, to))?)
    }

    /// Overwrite the part of the value stored in key at the specified offset.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn setrange<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        offset: isize,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::setrange(key, offset, value))?)
    }

    /// Delete one or more keys.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn del<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::del(key))?)
    }

    /// Determine if a key exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn exists<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::exists(key))?)
    }

    /// Determine the type of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn key_type<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::key_type(key))?)
    }

    /// Set a key's time to live in seconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn expire<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        seconds: i64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::expire(key, seconds))?)
    }

    /// Set the expiration for a key as a UNIX timestamp.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn expire_at<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        ts: i64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::expire_at(key, ts))?)
    }

    /// Set a key's time to live in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pexpire<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        ms: i64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pexpire(key, ms))?)
    }

    /// Set the expiration for a key as a UNIX timestamp in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pexpire_at<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        ts: i64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pexpire_at(key, ts))?)
    }

    /// Remove the expiration from a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn persist<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::persist(key))?)
    }

    /// Get the expiration time of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn ttl<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::ttl(key))?)
    }

    /// Get the expiration time of a key in milliseconds.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pttl<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pttl(key))?)
    }

    /// Get the value of a key and set expiration
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get_ex<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        expire_at: Expiry,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::get_ex(key, expire_at))?)
    }

    /// Get the value of a key and delete it
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn get_del<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::get_del(key))?)
    }

    /// Rename a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rename<'a, K: ToRedisArgs, N: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        new_key: N,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rename(key, new_key))?)
    }

    /// Rename a key, only if the new key does not exist.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rename_nx<'a, K: ToRedisArgs, N: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        new_key: N,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rename_nx(key, new_key))?)
    }

    /// Unlink one or more keys.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn unlink<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::unlink(key))?)
    }

    /// Append a value to a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn append<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::append(key, value))?)
    }

    /// Increment the numeric value of a key by the given amount.  This
    /// issues a `INCRBY` or `INCRBYFLOAT` depending on the type.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn incr<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        delta: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::incr(key, delta))?)
    }

    /// Decrement the numeric value of a key by the given amount.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn decr<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        delta: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::decr(key, delta))?)
    }

    /// Sets or clears the bit at offset in the string value stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn setbit<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        offset: usize,
        value: bool,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::setbit(key, offset, value))?)
    }

    /// Returns the bit value at offset in the string value stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn getbit<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        offset: usize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::getbit(key, offset))?)
    }

    /// Count set bits in a string.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bitcount<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bitcount(key))?)
    }

    /// Count set bits in a string in a range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bitcount_range<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: usize,
        end: usize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bitcount_range(key, start, end))?)
    }

    /// Perform a bitwise AND between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_and<'a, D: ToRedisArgs, S: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bit_and(dstkey, srckeys))?)
    }

    /// Perform a bitwise OR between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_or<'a, D: ToRedisArgs, S: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bit_or(dstkey, srckeys))?)
    }

    /// Perform a bitwise XOR between multiple keys (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_xor<'a, D: ToRedisArgs, S: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bit_xor(dstkey, srckeys))?)
    }

    /// Perform a bitwise NOT of the key (containing string values)
    /// and store the result in the destination key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bit_not<'a, D: ToRedisArgs, S: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        srckey: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bit_not(dstkey, srckey))?)
    }

    /// Get the length of the value stored in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn strlen<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::strlen(key))?)
    }

    /// Gets a single (or multiple) fields from a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hget<'a, K: ToRedisArgs, F: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hget(key, field))?)
    }

    /// Deletes a single (or multiple) fields from a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hdel<'a, K: ToRedisArgs, F: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hdel(key, field))?)
    }

    /// Sets a single field in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset<'a, K: ToRedisArgs, F: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hset(key, field, value))?)
    }

    /// Sets a single field in a hash if it does not exist.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset_nx<'a, K: ToRedisArgs, F: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hset_nx(key, field, value))?)
    }

    /// Sets a multiple fields in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hset_multiple<'a, K: ToRedisArgs, F: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        items: &'a [(F, V)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hset_multiple(key, items))?)
    }

    /// Increments a value.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hincr<'a, K: ToRedisArgs, F: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
        delta: D,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hincr(key, field, delta))?)
    }

    /// Checks if a field in a hash exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hexists<'a, K: ToRedisArgs, F: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        field: F,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hexists(key, field))?)
    }

    /// Gets all the keys in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hkeys<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hkeys(key))?)
    }

    /// Gets all the values in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hvals<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hvals(key))?)
    }

    /// Gets all the fields and values in a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hgetall<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hgetall(key))?)
    }

    /// Gets the length of a hash.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn hlen<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::hlen(key))?)
    }

    /// Pop an element from a list, push it to another list
    /// and return it; or block until one is available
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blmove<'a, S: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        srckey: S,
        dstkey: D,
        src_dir: Direction,
        dst_dir: Direction,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::blmove(srckey, dstkey, src_dir, dst_dir, timeout))?,
        )
    }

    /// Pops `count` elements from the first non-empty list key from the list of
    /// provided key names; or blocks until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blmpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        timeout: f64,
        numkeys: usize,
        key: K,
        dir: Direction,
        count: usize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::blmpop(timeout, numkeys, key, dir, count))?,
        )
    }

    /// Remove and get the first element in a list, or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn blpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::blpop(key, timeout))?)
    }

    /// Remove and get the last element in a list, or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn brpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::brpop(key, timeout))?)
    }

    /// Pop a value from a list, push it to another list and return it;
    /// or block until one is available.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn brpoplpush<'a, S: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        srckey: S,
        dstkey: D,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::brpoplpush(srckey, dstkey, timeout))?,
        )
    }

    /// Get an element from a list by its index.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lindex<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        index: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lindex(key, index))?)
    }

    /// Insert an element before another element in a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn linsert_before<'a, K: ToRedisArgs, P: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pivot: P,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::linsert_before(key, pivot, value))?)
    }

    /// Insert an element after another element in a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn linsert_after<'a, K: ToRedisArgs, P: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pivot: P,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::linsert_after(key, pivot, value))?)
    }

    /// Returns the length of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn llen<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::llen(key))?)
    }

    /// Pop an element a list, push it to another list and return it
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lmove<'a, S: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        srckey: S,
        dstkey: D,
        src_dir: Direction,
        dst_dir: Direction,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::lmove(srckey, dstkey, src_dir, dst_dir))?,
        )
    }

    /// Pops `count` elements from the first non-empty list key from the list of
    /// provided key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lmpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        numkeys: usize,
        key: K,
        dir: Direction,
        count: usize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lmpop(numkeys, key, dir, count))?)
    }

    /// Removes and returns the up to `count` first elements of the list stored at key.
    ///
    /// If `count` is not specified, then defaults to first element.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: Option<core::num::NonZeroUsize>,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lpop(key, count))?)
    }

    /// Returns the index of the first matching value of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpos<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
        options: LposOptions,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lpos(key, value, options))?)
    }

    /// Insert all the specified values at the head of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpush<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lpush(key, value))?)
    }

    /// Inserts a value at the head of the list stored at key, only if key
    /// already exists and holds a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lpush_exists<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lpush_exists(key, value))?)
    }

    /// Returns the specified elements of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lrange<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lrange(key, start, stop))?)
    }

    /// Removes the first count occurrences of elements equal to value
    /// from the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lrem<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: isize,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lrem(key, count, value))?)
    }

    /// Trim an existing list so that it will contain only the specified
    /// range of elements specified.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn ltrim<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::ltrim(key, start, stop))?)
    }

    /// Sets the list element at index to value
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn lset<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        index: isize,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::lset(key, index, value))?)
    }

    /// Removes and returns the up to `count` last elements of the list stored at key
    ///
    /// If `count` is not specified, then defaults to last element.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpop<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: Option<core::num::NonZeroUsize>,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rpop(key, count))?)
    }

    /// Pop a value from a list, push it to another list and return it.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpoplpush<'a, K: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        dstkey: D,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rpoplpush(key, dstkey))?)
    }

    /// Insert all the specified values at the tail of the list stored at key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpush<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rpush(key, value))?)
    }

    /// Inserts value at the tail of the list stored at key, only if key
    /// already exists and holds a list.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn rpush_exists<'a, K: ToRedisArgs, V: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        value: V,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::rpush_exists(key, value))?)
    }

    /// Add one or more members to a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sadd<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sadd(key, member))?)
    }

    /// Get the number of members in a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn scard<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::scard(key))?)
    }

    /// Subtract multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sdiff<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, keys: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sdiff(keys))?)
    }

    /// Subtract multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sdiffstore<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sdiffstore(dstkey, keys))?)
    }

    /// Intersect multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sinter<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, keys: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sinter(keys))?)
    }

    /// Intersect multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sinterstore<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sinterstore(dstkey, keys))?)
    }

    /// Determine if a given value is a member of a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sismember<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sismember(key, member))?)
    }

    /// Determine if given values are members of a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smismember<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        members: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::smismember(key, members))?)
    }

    /// Get all the members in a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smembers<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::smembers(key))?)
    }

    /// Move a member from one set to another.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn smove<'a, S: ToRedisArgs, D: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        srckey: S,
        dstkey: D,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::smove(srckey, dstkey, member))?)
    }

    /// Remove and return a random member from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn spop<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::spop(key))?)
    }

    /// Get one random member from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srandmember<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::srandmember(key))?)
    }

    /// Get multiple random members from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srandmember_multiple<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: usize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::srandmember_multiple(key, count))?)
    }

    /// Remove one or more members from a set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn srem<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::srem(key, member))?)
    }

    /// Add multiple sets.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sunion<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, keys: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sunion(keys))?)
    }

    /// Add multiple sets and store the resulting set in a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn sunionstore<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::sunionstore(dstkey, keys))?)
    }

    /// Add one member to a sorted set, or update its score if it already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zadd<'a, K: ToRedisArgs, S: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
        score: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zadd(key, member, score))?)
    }

    /// Add multiple members to a sorted set, or update its score if it already exists.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zadd_multiple<'a, K: ToRedisArgs, S: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        items: &'a [(S, M)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zadd_multiple(key, items))?)
    }

    /// Get the number of members in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zcard<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zcard(key))?)
    }

    /// Count the members in a sorted set with scores within the given values.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zcount<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zcount(key, min, max))?)
    }

    /// Increments the member in a sorted set at key by delta.
    /// If the member does not exist, it is added with delta as its score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zincr<'a, K: ToRedisArgs, M: ToRedisArgs, D: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
        delta: D,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zincr(key, member, delta))?)
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using SUM as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zinterstore(dstkey, keys))?)
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using MIN as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_min<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zinterstore_min(dstkey, keys))?)
    }

    /// Intersect multiple sorted sets and store the resulting sorted set in
    /// a new key using MAX as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_max<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zinterstore_max(dstkey, keys))?)
    }

    /// [`Commands::zinterstore`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zinterstore_weights(dstkey, keys))?)
    }

    /// [`Commands::zinterstore_min`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_min_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zinterstore_min_weights(dstkey, keys))?,
        )
    }

    /// [`Commands::zinterstore_max`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zinterstore_max_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zinterstore_max_weights(dstkey, keys))?,
        )
    }

    /// Count the number of members in a sorted set between a given lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zlexcount<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zlexcount(key, min, max))?)
    }

    /// Removes and returns the member with the highest score in a sorted set.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzpopmax<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bzpopmax(key, timeout))?)
    }

    /// Removes and returns up to count members with the highest scores in a sorted set
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zpopmax<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zpopmax(key, count))?)
    }

    /// Removes and returns the member with the lowest score in a sorted set.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzpopmin<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        timeout: f64,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bzpopmin(key, timeout))?)
    }

    /// Removes and returns up to count members with the lowest scores in a sorted set
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zpopmin<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zpopmin(key, count))?)
    }

    /// Removes and returns up to count members with the highest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzmpop_max<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        timeout: f64,
        keys: &'a [K],
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bzmpop_max(timeout, keys, count))?)
    }

    /// Removes and returns up to count members with the highest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zmpop_max<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        keys: &'a [K],
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zmpop_max(keys, count))?)
    }

    /// Removes and returns up to count members with the lowest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    /// Blocks until a member is available otherwise.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn bzmpop_min<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        timeout: f64,
        keys: &'a [K],
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::bzmpop_min(timeout, keys, count))?)
    }

    /// Removes and returns up to count members with the lowest scores,
    /// from the first non-empty sorted set in the provided list of key names.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zmpop_min<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        keys: &'a [K],
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zmpop_min(keys, count))?)
    }

    /// Return up to count random members in a sorted set (or 1 if `count == None`)
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrandmember<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: Option<isize>,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrandmember(key, count))?)
    }

    /// Return up to count random members in a sorted set with scores
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrandmember_withscores<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrandmember_withscores(key, count))?)
    }

    /// Return a range of members in a sorted set, by index
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrange<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrange(key, start, stop))?)
    }

    /// Return a range of members in a sorted set, by index with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrange_withscores<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrange_withscores(key, start, stop))?,
        )
    }

    /// Return a range of members in a sorted set, by lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebylex<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrangebylex(key, min, max))?)
    }

    /// Return a range of members in a sorted set, by lexicographical
    /// range with offset and limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebylex_limit<
        'a,
        K: ToRedisArgs,
        M: ToRedisArgs,
        MM: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrangebylex_limit(key, min, max, offset, count))?,
        )
    }

    /// Return a range of members in a sorted set, by lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebylex<'a, K: ToRedisArgs, MM: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrevrangebylex(key, max, min))?)
    }

    /// Return a range of members in a sorted set, by lexicographical
    /// range with offset and limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebylex_limit<
        'a,
        K: ToRedisArgs,
        MM: ToRedisArgs,
        M: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrevrangebylex_limit(key, max, min, offset, count))?,
        )
    }

    /// Return a range of members in a sorted set, by score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrangebyscore(key, min, max))?)
    }

    /// Return a range of members in a sorted set, by score with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_withscores<
        'a,
        K: ToRedisArgs,
        M: ToRedisArgs,
        MM: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrangebyscore_withscores(key, min, max))?,
        )
    }

    /// Return a range of members in a sorted set, by score with limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_limit<
        'a,
        K: ToRedisArgs,
        M: ToRedisArgs,
        MM: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrangebyscore_limit(key, min, max, offset, count))?,
        )
    }

    /// Return a range of members in a sorted set, by score with limit with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrangebyscore_limit_withscores<
        'a,
        K: ToRedisArgs,
        M: ToRedisArgs,
        MM: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        min: M,
        max: MM,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrangebyscore_limit_withscores(
                key, min, max, offset, count,
            ))?,
        )
    }

    /// Determine the index of a member in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrank<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrank(key, member))?)
    }

    /// Remove one or more members from a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrem<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        members: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrem(key, members))?)
    }

    /// Remove all members in a sorted set between the given lexicographical range.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrembylex<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrembylex(key, min, max))?)
    }

    /// Remove all members in a sorted set within the given indexes.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zremrangebyrank<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zremrangebyrank(key, start, stop))?)
    }

    /// Remove all members in a sorted set within the given scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrembyscore<'a, K: ToRedisArgs, M: ToRedisArgs, MM: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        min: M,
        max: MM,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrembyscore(key, min, max))?)
    }

    /// Return a range of members in a sorted set, by index, with scores
    /// ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrange<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrevrange(key, start, stop))?)
    }

    /// Return a range of members in a sorted set, by index, with scores
    /// ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrange_withscores<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        start: isize,
        stop: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrevrange_withscores(key, start, stop))?,
        )
    }

    /// Return a range of members in a sorted set, by score.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore<'a, K: ToRedisArgs, MM: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrevrangebyscore(key, max, min))?)
    }

    /// Return a range of members in a sorted set, by score with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_withscores<
        'a,
        K: ToRedisArgs,
        MM: ToRedisArgs,
        M: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        max: MM,
        min: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrevrangebyscore_withscores(key, max, min))?,
        )
    }

    /// Return a range of members in a sorted set, by score with limit.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_limit<
        'a,
        K: ToRedisArgs,
        MM: ToRedisArgs,
        M: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zrevrangebyscore_limit(key, max, min, offset, count))?,
        )
    }

    /// Return a range of members in a sorted set, by score with limit with scores.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrangebyscore_limit_withscores<
        'a,
        K: ToRedisArgs,
        MM: ToRedisArgs,
        M: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        key: K,
        max: MM,
        min: M,
        offset: isize,
        count: isize,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(
            Cmd::zrevrangebyscore_limit_withscores(key, max, min, offset, count),
        )?)
    }

    /// Determine the index of a member in a sorted set, with scores ordered from high to low.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zrevrank<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zrevrank(key, member))?)
    }

    /// Get the score associated with the given member in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zscore<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        member: M,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zscore(key, member))?)
    }

    /// Get the scores associated with multiple members in a sorted set.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zscore_multiple<'a, K: ToRedisArgs, M: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        members: &'a [M],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zscore_multiple(key, members))?)
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using SUM as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zunionstore(dstkey, keys))?)
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using MIN as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_min<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zunionstore_min(dstkey, keys))?)
    }

    /// Unions multiple sorted sets and store the resulting sorted set in
    /// a new key using MAX as aggregation function.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_max<'a, D: ToRedisArgs, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        keys: &'a [K],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zunionstore_max(dstkey, keys))?)
    }

    /// [`Commands::zunionstore`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::zunionstore_weights(dstkey, keys))?)
    }

    /// [`Commands::zunionstore_min`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_min_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zunionstore_min_weights(dstkey, keys))?,
        )
    }

    /// [`Commands::zunionstore_max`], but with the ability to specify a
    /// multiplication factor for each sorted set by pairing one with each key
    /// in a tuple.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn zunionstore_max_weights<
        'a,
        D: ToRedisArgs,
        K: ToRedisArgs,
        W: ToRedisArgs,
        RV: FromRedisValue,
    >(
        &mut self,
        dstkey: D,
        keys: &'a [(K, W)],
    ) -> RedisResult<RV> {
        from_owned_redis_value(
            self.glide_send_owned_sync(Cmd::zunionstore_max_weights(dstkey, keys))?,
        )
    }

    /// Adds the specified elements to the specified HyperLogLog.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfadd<'a, K: ToRedisArgs, E: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        element: E,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pfadd(key, element))?)
    }

    /// Return the approximated cardinality of the set(s) observed by the
    /// HyperLogLog at key(s).
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfcount<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pfcount(key))?)
    }

    /// Merge N different HyperLogLogs into a single one.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn pfmerge<'a, D: ToRedisArgs, S: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        dstkey: D,
        srckeys: S,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::pfmerge(dstkey, srckeys))?)
    }

    /// Posts a message to the given channel.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn publish<'a, K: ToRedisArgs, E: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        channel: K,
        message: E,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::publish(channel, message))?)
    }

    /// Returns the encoding of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_encoding<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::object_encoding(key))?)
    }

    /// Returns the time in seconds since the last access of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_idletime<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::object_idletime(key))?)
    }

    /// Returns the logarithmic access frequency counter of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_freq<'a, K: ToRedisArgs, RV: FromRedisValue>(&mut self, key: K) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::object_freq(key))?)
    }

    /// Returns the reference count of a key.
    #[inline]
    #[allow(deprecated)]
    #[allow(clippy::extra_unused_lifetimes, clippy::needless_lifetimes)]
    fn object_refcount<'a, K: ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<RV> {
        from_owned_redis_value(self.glide_send_owned_sync(Cmd::object_refcount(key))?)
    }

    /// Incrementally iterate the keys space.
    #[inline]
    fn scan<RV: FromRedisValue>(&mut self) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("SCAN");
        c.cursor_arg(0);
        c.iter(self)
    }

    /// Incrementally iterate the keys space for keys matching a pattern.
    #[inline]
    fn scan_match<P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        pattern: P,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("SCAN");
        c.cursor_arg(0).arg("MATCH").arg(pattern);
        c.iter(self)
    }

    /// Incrementally iterate hash fields and associated values.
    #[inline]
    fn hscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("HSCAN");
        c.arg(key).cursor_arg(0);
        c.iter(self)
    }

    /// Incrementally iterate hash fields and associated values for field
    /// names matching a pattern.
    #[inline]
    fn hscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("HSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        c.iter(self)
    }

    /// Incrementally iterate set elements.
    #[inline]
    fn sscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("SSCAN");
        c.arg(key).cursor_arg(0);
        c.iter(self)
    }

    /// Incrementally iterate set elements for elements matching a pattern.
    #[inline]
    fn sscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("SSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        c.iter(self)
    }

    /// Incrementally iterate sorted set elements.
    #[inline]
    fn zscan<K: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("ZSCAN");
        c.arg(key).cursor_arg(0);
        c.iter(self)
    }

    /// Incrementally iterate sorted set elements for elements matching a
    /// pattern.
    #[inline]
    fn zscan_match<K: redis::ToRedisArgs, P: redis::ToRedisArgs, RV: FromRedisValue>(
        &mut self,
        key: K,
        pattern: P,
    ) -> RedisResult<redis::Iter<'_, RV>> {
        let mut c = cmd("ZSCAN");
        c.arg(key).cursor_arg(0).arg("MATCH").arg(pattern);
        c.iter(self)
    }
}
